# Override default environment variables here
# This file is ignored by git, so you can safely put sensitive information

# Generate a strong secret key with: openssl rand -base64 42
SUPERSET_SECRET_KEY=your-very-strong-secret-key-here-change-this

# Set to 'no' if you don't want example data
SUPERSET_LOAD_EXAMPLES=yes

# Database Configuration (if different from defaults)
# DATABASE_PASSWORD=your-custom-password

# Redis Configuration (using external Redis instance)
REDIS_HOST=redis_prod
REDIS_PORT=6379
REDIS_PASSWORD=CQhgg6uELsQQwUXA

# Superset Configuration
SUPERSET_PORT=8088

# For production, set these
# FLASK_ENV=production
# SUPERSET_ENV=production

# Mapbox API Key for map visualizations
# MAPBOX_API_KEY=your-mapbox-api-key

# Application root path (if running under subdirectory)
# SUPERSET_APP_ROOT=/analytics